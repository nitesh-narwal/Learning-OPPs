package me.niteshh.OPPs.tutorial.collectionFramework.lecturePractice.List_Lecture;

public class Theory {
    /**
     * A collection is simply an object that represents  a group of objects
     * known as elements.
     *
     * It provides us classes and interface that helps to manage the group of objects.
     * Key Interface in the Collection Framework:
     * The Collection Framework is primarily built around the around a set of interfaces.
     * 1. Collection: The root interface in the collection hierarchy.
     *                It represents a group of objects known as elements.
     * 2. List: An ordered collection that allows duplicate elements.
     *          It provides positional access and insertion of elements.
     *          Common implementations include ArrayList, LinkedList, and Vector.
     * 3. Set: A collection that does not allow duplicate elements.
     *         It provides fast lookup and insertion of elements.
     *         Common implementations include HashSet, TreeSet, and EnumSet.
     * 4. Map: A collection that maps keys to values.
     *         It provides fast lookup and insertion of elements.
     *         Common implementations include HashMap, TreeMap, and EnumMap.
     * 5. Queue: A collection that provides first-in-first-out (FIFO) ordering of elements.
     *           Common implementations include ArrayDeque, LinkedList, and PriorityQueue.
     * 6. Deque: A double-ended queue (deque) that provides both FIFO and LIFO ordering of elements.
     *           Common implementations include ArrayDeque, LinkedList, and PriorityQueue.
     * 7. Stack: A last-in-first-out (LIFO) stack of elements.
     *           Common implementations include ArrayDeque, LinkedList, and Vector.
     * 8. SortedSet: A set that maintains its elements in sorted order.
     *               Common implementations include TreeSet, and EnumSet.
     * 9. SortedMap: A map that maintains its keys in sorted order.
     *               Common implementations include TreeMap, and EnumMap.
     * 10. Concurrent: A collection that provides concurrent access and modification.
     *                 Common implementations include CopyOnWriteArrayList, ConcurrentHashMap,
     *                 and ConcurrentSkipListSet.
     * 11. Other: Other specialized collections such as BitSet, EnumSet, and Properties.
     *
     *
     * Iterable Interface: The root interface in the collection hierarchy is the Iterable interface.
     *                      It provides a way to iterate over the elements of a collection using an iterator.
     * The Collection Interface: The Collection interface is the root interface in the collection hierarchy.
     *                           It represents a group of objects known as elements.
     *                           It provides methods for adding, removing, and querying elements in the collection.
     * The List Interface: The List interface is an ordered collection that allows duplicate elements.
     *                      It provides positional access and insertion of elements.
     *                      Common implementations include ArrayList, LinkedList, and Vector.
     * The Set Interface: The Set interface is a collection that does not allow duplicate elements.
     *                     It provides fast lookup and insertion of elements.
     *                     Common implementations include HashSet, TreeSet, and EnumSet.
     * The Map Interface: The Map interface is a collection that maps keys to values.
     *                     It provides fast lookup and insertion of elements.
     *                     Common implementations include HashMap, TreeMap, and EnumMap.
     * The Queue Interface: The Queue interface is a collection that provides first-in-first-out (FIFO) ordering of elements.
     *                      Common implementations include ArrayDeque, LinkedList, and PriorityQueue.
     * The Deque Interface: The Deque interface is a double-ended queue (deque) that provides both FIFO and LIFO ordering of elements.
     *                      Common implementations include ArrayDeque, LinkedList, and PriorityQueue.
     * The Stack Interface: The Stack interface is a last-in-first-out (LIFO) stack of elements.
     *                      Common implementations include ArrayDeque, LinkedList, and Vector.
     *
     * Any class which can implement this interface... we can use for-each loop for it's objects
     * */





}
